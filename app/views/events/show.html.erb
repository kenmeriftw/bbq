<div class="col-md-6 text-center">
  <div class="img-thumbnail">

    <%= content_tag 'div', '', class: 'event-background' %>

    <div class="thumbnail-inner event-info-inner">
      <div class="caption">
        <div class="event-description">
          <%= link_to @event.title, event_path(@event), class: "fs-5 text-decoration-none" %>
          <p class="fs-6 text-muted"><%= l @event.datetime, format: :long %></p>
          <div class="buffer-both"><%= @event.address %></div>
        </div>
      </div>
      <% if current_user_can_edit?(@event) %>
          <div class="buffer-top">
            <%= link_to 'Изменить', edit_event_path(@event), class: 'btn btn-outline-primary' %>
          </div>
      <% end %>
    </div>
  </div>
</div>

         

