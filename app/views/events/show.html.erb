<div class="col-md-8 text-center">
  <div class="img-thumbnail">

    <%= content_tag 'div', '', class: 'event-background' %>

    <div class="thumbnail-inner event-info-inner">
      <div class="caption">
        <div class="event-description">
          <%= link_to @event.title, event_path(@event), class: "fs-5 text-decoration-none" %>
          <p class="fs-6 text-muted"><%= l @event.datetime, format: :long %></p>
          <div class="buffer-both"><%= @event.address %></div>
        </div>
      </div>
      <% if current_user_can_edit?(@event) %>
        <div class="buffer-top">
          <%= link_to t('controllers.events.edit'), edit_event_path(@event), class: 'btn btn-outline-primary' %>
          <%= link_to t('controllers.events.delete'), @event, method: :delete, data: { confirm: t('views.sure') }, class: 'btn btn-outline-danger' %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-md-8">
      <div class="event-comments buffer-top">
        <%= render partial: 'comments/form',
                   locals: { comment: @new_comment || @event.comments.build } %>
        <div class="buffer-both">
          <%= render partial: 'comments/comment',
                     collection: @event.comments.except(@new_comment) %>
        </div>
      </div>
    </div>

    <div class="col-md-4">
      <div class="event-owner">
        <h4>Зовет:</h4>
        <p><%= render partial: 'users/user', object: @event.user %></p>
      </div>
      <div class="event-subscriptions">
        <h4>Пойдут:</h4>
        <div>
          <p>
            <%= render partial: 'subscriptions/subscription',
                       collection: @event.subscriptions.except(@new_subscription) %>
          </p>
        </div>
      </div>
      <div class="event-subscription-form">
        <h4>Я пойду:</h4>
        <div>
          <div>
          
            <%= render partial: 'subscriptions/form', locals: {
              subscription: @new_subscription || @event.subscriptions.build
            } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
